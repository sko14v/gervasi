/**
 * Re-exports de los tipos compartidos.
 * El frontend nunca importa de `@agentik-os/shared/src/...` —
 * siempre desde `@/types` para que mover el package no rompa imports.
 */

export * from '@agentik-os/shared';
